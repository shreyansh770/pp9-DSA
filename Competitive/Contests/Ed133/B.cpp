#include <bits/stdc++.h>
using namespace std;
int main(){


    int t;
    cin>> t;

    while(t--){

        int n;
        cin>>n;

        vector<int> arr(n);
        for(int i=0;i<n;i++){
            arr[i] = i+1;
        }

        vector<

        cout<< n;

        bool flag = true;
        int swapIdx = 0;
        while(n--){

            for(int e : a){
                cout<<e<<' ';
            }

            cout<<'\n';

            if(n){
                swap(arr[swapIdx],arr[swapIdx+1]);
            }

            swapIdx++;

        }

         if(flag) flag =!flag;
    }
}